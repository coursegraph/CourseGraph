import React from 'react';
import PropTypes from 'prop-types';

import TextField from 'material-ui/TextField';

/**
 *
 * @param onSubmit
 * @param onChange
 * @param errors
 * @param successMessage
 * @param user
 * @param toggleAuthenticateStatus
 * @return {Element}
 * @constructor
 */
const LoginForm = ({
                     onSubmit,
                     onChange,
                     errors,
                     successMessage,
                     user,
                     toggleAuthenticateStatus
                   }) => (
  <div>

  </div>
);


export default LoginForm;
